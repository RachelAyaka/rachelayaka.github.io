import { FieldStatus } from '@/types/FieldStatus';
import Status from './Status';

const IDLE_FIELD_STATUS: FieldStatus = {
  status: Status.IDLE,
  errorMessage: '',
};

export default IDLE_FIELD_STATUS;
